{
  "name": "knm-audio",
  "private": true,
  "description": "Netlify Function dependencies for the PIN-sync backend (T-10). The site itself is a static index.html, no build step.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
