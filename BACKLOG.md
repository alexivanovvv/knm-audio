# Backlog — knm-audio

## T-01 — Редизайн под конвенции аудиокниг
Текущий UI (dark + JetBrains Mono + study-ring) выглядит как дашборд, не как
аудиокнига. Переверстать по референсам реальных audiobook-приложений
(Audible, Spotify Audiobooks, Libro.fm, Storytel): чистая обложка/hero текущей
главы, вертикальный список глав вместо горизонтальной обрезающейся полки,
консистентные брейкпоинты для десктопа и мобилки. CSS сейчас захламлён
несколькими наслоившимися патчами — почистить при переверстке.
Status: **done** — единая тёмная/светлая тема, вертикальный список глав с
обложками вместо горизонтальной полки, карточка "Verder luisteren", убрана
мёртвая rail/sidebar-вёрстка и study-ring.

## T-02 — Проверить, что вся озвучка на ElevenLabs
README и коммиты показывают: только главы 1-2 + H5T5 (11 из 36 треков)
переозвучены через ElevenLabs Flash, остальные 25 — всё ещё edge-tts
(Microsoft neural, `nl-NL-MaartenNeural`/`en-US-GuyNeural`). Нет
ElevenLabs API-ключа в системе → перегенерация остальных 25 треков требует
ключа и стоит денег (ElevenLabs тарифицирует по символам).
Status: **done** — все 36 треков теперь на ElevenLabs Flash (`eleven_flash_v2_5`,
voice George `JBFqnCBsd6RMkjVDRZzb`, те же voice_settings, что и у первых 11
треков). Оставшиеся 25 (H3-H7) сгенерированы по сегментам EN/NL (границы
`[Dutch term]`) и склеены через ffmpeg. Потрачено ~67 300 символов, квота
ElevenLabs осталась в пределах лимита (без овербиллинга).

## T-03 — Перенести деплой с GitHub Pages на Netlify
По аналогии с hdash.netlify.app / knm-quiz.netlify.app: отдельный Netlify-сайт,
репозиторий остаётся на GitHub, деплой — `netlify deploy --prod`.
Status: **done**

## T-04 — Пуш после каждого изменения, деплой — по накоплению фич
Изначально: любое значимое изменение → commit + push + `netlify deploy --prod`.
Пересмотрено 2026-09-24 после исчерпания кредитов Netlify (несколько
параллельных сессий деплоили слишком часто в один день): теперь commit + push
на GitHub — после каждого изменения (бесплатно), а `netlify deploy --prod` —
только когда накопилось достаточно новых фич, вручную. Для этого добавлен
пункт "Deploy to Netlify" в выпадающем меню настроек (⋮ в хедере, виден
только на `localhost`/`127.0.0.1`, полностью отсутствует на проде):
запусти `python3 dev-server.py` (порт 8765) и открой `http://localhost:8765`
— сервер отдаёт статику и слушает `POST /__deploy`, который дергает
`netlify deploy --prod --dir=.`. Пункт меню спрашивает подтверждение перед
реальным деплоем.
Status: **ongoing convention**, обновлено 2026-09-24.

## T-08 — Меню настроек, i18n, Spotify-стиль плеера, анонс глав
Пять правок в один заход:
- Плеер снизу переверстан под "Spotify"-стиль: SVG-иконки транспорта вместо
  emoji/юникода, бо́льшая обложка (52px), тоньше scrubber, крупнее play.
- Все настройки (тема, язык, автоплей, анонс, пауза между главами, hard
  refresh, deploy) собраны в одном выпадающем меню (⋮) вместо разбросанных
  чекбоксов/кнопок.
- Интерфейс (не контент книги) теперь на английском по умолчанию, с
  переключателем на нидерландский в меню — EN/NL заголовки задач/глав
  остаются двуязычными как раньше, это отдельная вещь.
- Автоплей — теперь пункт меню (чекбокс-стиль), а не чекбокс в `.overview`.
- Субтитры (CC) больше не перекрывают плеер — `.player` теперь поверх
  `.captions-drawer` по z-index, в скролле субтитров добавлен запас снизу.
- Новое: опциональный чайм + голосовое объявление "5.2. What kind of work
  suits you?" (Web Speech API, без доп. аудиофайлов) перед началом каждого
  трека — тумблер в меню, выключено по умолчанию.
- Новое: настраиваемая пауза между главами (Выкл / 2с / 5с, тумблер в меню)
  вместо жёстких 3 секунд.
Status: **done**, не задеплоено — ждёт накопления следующей пачки фич (T-04).

## T-05 — Режим субтитров/lyrics при воспроизведении
Кнопка "CC" в плеере открывает полноэкранный drawer с построчной
синхронизированной расшифровкой текущей таки (EN + `[NL термины]`
подсвечены), автоскролл к активной строке, тап по строке — перемотка.
Timing получен forced alignment'ом (pocketsphinx, офлайн, без скачивания
моделей — HF/Azure/ModelScope и т.п. заблокированы политикой песочницы;
en-us акустическая модель идёт прямо в pip-пакете `pocketsphinx`) реального
исходного текста ("Welkom in Nederland — Voiceover Script (EN-NL)" из
Dropbox/Obsidian) к аудио, с рукописным NL→ARPAbet g2p для 859 голландских
слов/терминов вне словаря.
Status: **done — 36/36 таков имеют `subtitles/H{ch}T{t}.json`**, но с двумя
уровнями точности:
- **23/36 (`estimated: false` или отсутствует)** — точный word-level forced
  alignment, посегментно сверенный на пословное совпадение.
- **13/36 (`estimated: true`)** — H1T1-6, H2T1-4, H4T2, H5T5, H7T2
  (практически ровно "первые 11" ElevenLabs-треков из T-02, плюс 2). У этих
  треков forced alignment систематически "расползается": при полной
  подаче текста падает на "Final result does not match the grammar" у
  самого конца трека, а при попытке скормить окнами/чанками — смещения
  накапливаются от чанка к чанку (аудио физически не хватает под конец),
  похоже на артефакт более раннего конкат-пайплайна этой партии, не на
  проблему текста (пробовал recursive-bisection, sequential chunking с
  разными размерами окна/буфера — `align_robust.py`/`align_chunked.py` в
  scratchpad, не в репо). Для них подставлена таймингов-оценка
  (пропорционально длине текста, отмасштабированная на реальную
  длительность трека) — в плеере на таких таках показывается пометка "Bij
  benadering gesynchroniseerd" над списком строк, чтобы не выдавать
  оценку за точную синхронизацию. CC-кнопка везде активна.
Дальше, если понадобится дотянуть точность этих 13: раздобыть где-то
API-ключ ElevenLabs (there's `with_timestamps`, даёт word-level тайминги
прямо от синтеза) или прогнать более сильную ASR-модель вне песочницы
(см. историю в T-06) и импортировать результат.

## T-06 — Скачивание ASR-моделей заблокировано политикой песочницы
Для T-05 планировался forced alignment через `faster-whisper` (лучше
справляется со смешанным EN/NL текстом), но huggingface.co,
openaipublic.azureedge.net, models.silero.ai, alphacephei.com,
ggml.ggerganov.com, modelscope.cn — всё вернуло 403 от egress-прокси
песочницы (allowlist пропускает только github.com/gitlab.com/pypi.org/npm
и т.п. код-хостинги). В итоге T-05 сделан на `pocketsphinx` (pip-пакет
несёт en-us акустическую модель прямо в wheel, без внешних скачиваний).
Status: **не блокирует** — обходной путь найден, но если когда-нибудь
понадобится более точная модель, придётся либо гонять её за пределами
этой песочницы (локально на Mac) и импортировать тайминги, либо просить
администратора добавить нужный хост в allowlist.

## T-07 — Брендинг: иконка, description, social image, titles
Старая иконка была generic (оранжевый градиент + "NL" + плоская полоска
триколора). Сделан цельный визуальный язык: серифный wordmark "NL" +
эквалайзер-бары в цветах NL-триколора (читается и как "аудио", и как
"Нидерланды") на том же оранжевом градиенте, что и accent-цвет приложения.
Status: **done** — перегенерирован icons/ (32/180/192/512 + maskable),
добавлены social/og-image.png (1200×630) и social/cover-1080.png
(1080×1080); index.html получил og:*/twitter:* мета-теги и favicon-ссылку;
manifest.webmanifest — обновлённые name/short_name/description в тон
audiobook-брендингу; sw.js — bump SHELL_CACHE до v5, чтобы старые установки
подтянули новые иконки.
